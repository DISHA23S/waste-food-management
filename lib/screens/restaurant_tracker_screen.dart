import 'package:flutter/material.dart';

class RestaurantTrackerScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Restaurant Tracker")),
      body: Center(child: Text("Tracking functionality coming soon!")),
    );
  }
}
