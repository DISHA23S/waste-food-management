import 'package:flutter/material.dart';

class FoodTransferScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Food Transfer")),
      body: Center(child: Text("Food transfer details coming soon!")),
    );
  }
}
